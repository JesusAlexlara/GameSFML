#ifndef ANIMATION_H
#define ANIMATION_H

#include <SFML/Graphics.hpp>
#include <SFML/Window.hpp>
#include <string>

class Animation : public sf::Drawable
{
	public:
		Animation();
		~Animation();
		
		/*virtual void LoadContent(std::string txt, sf::Texture img, sf::Vector2f pos);
		virtual void UnloadContent();
		virtual void Update();
		virtual void draw(sf::RenderTarget& target, sf::RenderStates states) const;*/
		
	protected:	
	private:
		/*float alpha;
		sf::Text text;
		sf::Texture image;
		sf::Sprite sprite;
		sf::Vector2f position;
		sf::Color textColor;
		
		sf::IntRect sourceRect;*/
		
		
};

#endif
