#include "Animation.h"

Animation::Animation()
{
}

Animation::~Animation()
{
}

/*void Animation::LoadContent(std::string txt, sf::Texture img, sf::Vector2f pos)
{
	this->text.setString(txt);
	this->image = img;
	this->position = pos;
	
	this->alpha = 1.0f;
	this->textColor = sf::Color(144, 50,78);
	this->sprite.setTexture(this->image);
}

void Animation::UnloadContent(){}

void Animation::Update(){}

void Animation::draw(sf::RenderTarget &target, sf::RenderStates states) const
{
	if(this->text.getString() != "")
		target.draw(text);
		
	if(this->sprite.getTexture() != NULL)
		target.draw(sprite);
}*/
